name: Daily forward validation

on:
  workflow_dispatch:
  schedule:
    - cron: "30 17 * * 1-5"
      timezone: "Asia/Taipei"

permissions:
  contents: write

jobs:
  update-forward-log:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install tracker dependencies
        run: python -m pip install -r requirements-tracker.txt

      - name: Run daily forward tracker
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
        run: python forward_tracker.py

      - name: Commit forward log when changed
        shell: bash
        run: |
          if git diff --quiet -- data/forward_signals.csv; then
            echo "No forward-log change to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/forward_signals.csv
          git commit -m "chore: update forward validation log"
          git push
